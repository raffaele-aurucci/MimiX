import 'package:flutter/material.dart';

class PaletteColor {
  // For text, primary buttons, active icons, secondary buttons borders, selected input text, circular progression indicator.
  static const Color darkBlue = Color(0xFF003659);

  // For not active icons, standard progression indicator, placeholder text,
  static const Color lightSkyBlue = Color(0xFF97CADB);

  // For home-page cards, not selected input text.
  static const Color powderBlue = Color(0xFFEAFAFE);

  // For background color
  static const Color backgroundColor = Colors.white;

  // For using white color
  static const Color whiteColor = Colors.white;

  // For background of progress bar
  static const Color progressBarBackground = Color(0xFFF0F2F4);

  // For error messages
  static const Color errorMessage = Color(0xFFEB5858);
  
  // Yellow 
  static const Color yellow = Color(0xFFFFCE00);
}
